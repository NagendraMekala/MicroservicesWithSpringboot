spring:
  application:
    name: employee-service
  cloud:
    config:
      uri: http://localhost:8088

security:  basic:    enabled: false
management:
  security:
    enabled: false

      